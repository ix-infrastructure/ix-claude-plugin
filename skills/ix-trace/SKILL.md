---
name: ix-trace
description: Trace the execution flow or call chain for a symbol using Ix Memory
argument-hint: <symbol>
---

Run `ix trace $ARGUMENTS --format json` using the Bash tool.

Present the trace as a readable flow showing how data or control moves through the system from the given entry point, including key callsites and branching paths.
